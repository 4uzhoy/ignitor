git rm -r --cached . && git add . && git commit -am 'chore(git): reindex files' 
# Usage: reindex-git.bash
# Example: reindex-git.bash
# This script is used to reindex files in a git repository. And commit the changes with a message 'chore(git): reindex files'.