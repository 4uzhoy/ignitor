dart run build_runner build --delete-conflicting-outputs  
# Usage: codegen.bash
# Example: codegen.bash
# This script runs the Dart build runner to generate code based on the current project's configuration.