name: client
description: http client for network requests
version: 1.0.0
homepage: "none"
publish_to: 'none'
environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.27.0"

resolution: workspace


dependencies:
  flutter:
    sdk: flutter
  dio: ^5.5.0+1
  path: ^1.9.0
  meta: ^1.12.0
  http_parser: ^4.1.2
  common:
    path: ../common

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: any
  lints: ^5.0.0


